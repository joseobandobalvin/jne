import 'package:flutter/material.dart';

class DetailTrajectory extends StatelessWidget {
  const DetailTrajectory({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
